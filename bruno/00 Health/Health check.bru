meta {
  name: Health check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

docs {
  # GET /api/v1/health

  Confirms the API process is up.

  ## Example response `200`
  ```json
  {
    "success": true,
    "status": "ok"
  }
  ```
}

assert {
  res.status: eq 200
}
